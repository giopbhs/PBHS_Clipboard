<%= form_for([@snipit, @snipit.comments.build]) do |f| %>
	<p>
		<%= f.label :name %> <br/>
		<%= f.text_field :name %>
	</p>
	<p>
		<%= f.label :body %><br/>
		<%= f.text_area :body %>
	</p>
	<br/>
	<p>
		<%= f.submit %>
	</p>
<% end %>